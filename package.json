{
  "private": true,
  "scripts": {
    "build": "spago build",
    "pulp-build": "pulp build",
    "example": "spago -x ./example.dhall bundle-app --main Example.Main --to example/dist/metronome.js"
  },
  "devDependencies": {
    "pulp": "^16.0.0",
    "spago": "^0.20.9",
    "purescript": "^0.15.4",
    "purescript-psa": "^0.5.1"
  },
  "dependencies": {
    "esbuild": "^0.15.7"
  }
}
